{
  "name": "vueuse-playground-next",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --filter ./packages --parallel run dev",
    "build": "pnpm -r --filter ./packages run build",
    "client:dev": "pnpm -C ./packages/client run dev",
    "client:build": "pnpm -C ./packages/client run build",
    "client:serve": "pnpm -C ./packages/client run serve",
    "server:dev": "pnpm -C ./packages/server run dev",
    "server:build": "pnpm -C ./packages/server run build",
    "server:serve": "pnpm -C ./packages/server run serve",
    "types-server:dev": "pnpm -C ./packages/types-server run dev",
    "types-server:build": "pnpm -C ./packages/types-server run build",
    "types-server:serve": "pnpm -C ./packages/types-server run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wheatjs/vueuse-playground-next.git"
  },
  "keywords": [],
  "author": "Jacob Clevenger <jacobrclevenger@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wheatjs/vueuse-playground-next/issues"
  },
  "homepage": "https://github.com/wheatjs/vueuse-playground-next#readme",
  "devDependencies": {
    "@antfu/eslint-config": "^0.6.6",
    "eslint": "^7.30.0",
    "pnpm": "^6.10.1",
    "tsup": "^4.12.5"
  },
  "dependencies": {
    "automerge": "^1.0.1-preview.3",
    "dotenv": "^10.0.0",
    "vue-global-api": "^0.1.3"
  }
}
